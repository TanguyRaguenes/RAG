sonar.projectKey=TanguyRaguenes_RAG
sonar.organization=tanguyraguenes
sonar.sourceEncoding=UTF-8
sonar.python.coverage.reportPaths=rag_*/coverage.xml

# Tests are reviewed for maintainability but do not represent production duplication.
# Logging bootstrap stays local so every microservice image remains autonomous.
sonar.cpd.exclusions=**/tests/**,**/app/core/logging.py
