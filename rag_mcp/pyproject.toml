[project]
name = "rag-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]",
    "httpx",
    "uvicorn",
]

[dependency-groups]
dev = [
    "debugpy",
    "pytest>=9.0.2",
]
